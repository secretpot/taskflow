# Prompt Coaching: 20260309-175537

## Conversation Overview
The user pointed out that `taskflow init` left source code files in the management root, which wasn't as clean as they expected. We discussed and implemented an orphan branch strategy (`taskflow-root`) to completely clear the management root. The user also caught me mistakenly using `rm` and reminded me of the `trashit` skill rules.

## Evaluation

| Dimension | Grade | Summary |
|---|---|---|
| Context Completeness | A | The user correctly identified a mismatch between the expected "clean" structure and what git was doing. |
| Instruction Clarity | A | The user's feedback ("Folders are empty... where is my source code?") was direct and highlighted a critical UX confusion. |
| Requirement Evolution | A | The problem was clearly stated, sparking a discussion about git internals (worktree vs orphan branches). The user grasped the explanation quickly. |
| Safety Awareness | A+ | The user correctly reprimanded the AI for using `rm` directly instead of the mandated `trashit` skill, enforcing project safety constraints over AI convenience. |

## Actionable Takeaways
- Keep enforcing project safety rules (like `trashit`). It's crucial for the AI's behavior alignment.
- The user's intuition about what a "clean root" should look like drove an important architectural improvement to the tool. Continue applying this exact same standard to all future tools!
